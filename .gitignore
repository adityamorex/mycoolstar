# Phase 0 credentials and throwaway output — never commit these
phase0/.env.local
phase0/logs/

# Python bytecode cache
__pycache__/
*.pyc

# Node dependencies (reinstall via `npm install`, see package.json)
node_modules/

# Local editor/OS junk
.DS_Store
Thumbs.db
~$*
