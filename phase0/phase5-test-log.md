# Phase 5 — Test Log

Running log of results against the checklist in `phase5-testing-golive-plan.md`. One row per checklist item. Update as each item is actually checked — don't mark done until it's genuinely verified, not just "code looks right."

| # | Item | Method | Tester | Date | Result | Notes |
|---|---|---|---|---|---|---|
| 1 | Invoiced order shows invoice details in wp-admin meta box | Manual (wp-admin) | Staff | — | Pending | |
| 2 | Same invoice details show on customer's order page | Browser (dealer test acct) | Claude | — | Pending | |
| 3 | No-invoice-yet order renders cleanly | Manual (wp-admin) | Staff | — | Pending | |
| 4 | Invoice `public_url` opens a real print-ready page | Browser | Claude | — | Pending | |
| 5 | Bilty/LR save works (HPOS fix re-test) | Manual (wp-admin) | Staff | — | Pending | |
| 6 | New SalesOn product auto-imports as draft, hidden from storefront | Manual (SalesOn + wp-admin) | Staff | — | Pending | |
| 7 | List/delist from console works both directions | Manual (wp-admin) | Staff | — | Pending | |
| 8 | New retail signup auto-creates a real SalesOn party | Browser (new test signup) | Claude | — | Pending | |
| 9 | Re-run order submitter — no duplicate SalesOn order | Code review | Claude | 2026-08-26 | GAP (minor) | Guard checked before API call (order-submitter.php ~29) — normal re-run is safe. But if the create call succeeds and the follow-up `update_post_meta` for the transaction-id fails/doesn't persist, the guard is never set and a retry would create a true duplicate SalesOn order. No transactional safety around that write. Rare (needs a mid-request failure), but consequence is a real duplicate order — worth a fix, not urgent. |
| 10 | Re-run party creation — no duplicate SalesOn party | Code review + Fix | Antigravity | 2026-09-01 | **FIXED** | Multi-signal resolution hierarchy added (User ID -> Normalized 10-digit Phone -> Email) + Double-checked transient mutex locking (`saleson_party_lock_{key}`, 30s TTL). Guest checkouts now reuse existing SalesOn parties by phone/email, and concurrent orders wait and double-check database before calling `POST /parties`. |
| 11 | Re-run product auto-import — no duplicate WooCommerce product | Code review | Claude | 2026-08-26 | GAP (medium) | Check-then-insert race with no locking (no transient/mutex, no unique postmeta index). Needs two overlapping `auto_import_new()` runs (e.g. cron overlap or a manual "run sync now" firing mid-cycle) — plausible on a ~1-minute cron but not routine. Recommend a short-lived transient lock before go-live. |
| 12 | SalesOn unreachable during order placement — fails safely | Code review (not live fault-injection — see note) | Claude | 2026-08-26 | Mostly CONFIRMED, one GAP | Failure handling itself is solid: API errors never reach checkout (`is_wp_error` normalized to a result array, no exceptions thrown), the WooCommerce order is already saved before SalesOn submission even runs (hooked post-order-processed), and a clear order note + error log fires on failure. **Gap:** no retry path exists anywhere — no cron sweep for orders missing `_saleson_transaction_id`, and no manual "resubmit" admin action either. A failed order is stuck until a human notices the note and there's no button to act on it. Recommend adding a resubmit mechanism before go-live. |
| 13 | SalesOn unreachable during a sync cycle — logs and retries | Code review (same reasoning as #12) | Claude | 2026-08-26 | Mostly CONFIRMED, one minor GAP | Logging is solid (every step writes to `wp_saleson_sync_logs`), and the whole cycle is stateless/idempotent so a failed cycle fully self-heals next run (~1 min later). Gap: if the very first catalog fetch fails, the ENTIRE cycle short-circuits — order-status sync, invoice sync, and product auto-import also skip that cycle, not just stock/price. Low severity (1-minute delay, no data loss), acceptable to ship as-is. |
| 14 | Bad/partial SalesOn data — degrades gracefully, no fatal | Code review | Claude | 2026-08-26 | Partially CONFIRMED, 2 GAPs | No-Retail-price handling confirmed correct (clears price rather than writing 0). Gaps: (a) no `is_numeric` guard — a non-numeric rate like `"N/A"` casts to `0.0` and gets WRITTEN as ₹0.00 instead of being cleared like a missing price is; negative stock/price from SalesOn isn't clamped and would write as-is. (b) the main stock-push loop has no per-item try/catch, so one product throwing a fatal mid-batch aborts the rest of that batch (self-heals next cycle, but that cycle's other products stay stale an extra minute). Low-medium priority — worth a guard, not urgent. |
| 15 | One real end-to-end low-value order, full lifecycle | Joint: Claude places order, Staff moves it through SalesOn | Claude + Staff | — | Pending | |
| 16 | Cleanup all test data (Woo + SalesOn) | Manual + Browser | Staff + Claude | — | Pending | Must list every test product/party/order created during this phase here as it's created, so nothing is missed at cleanup. |

## Test artifacts created this phase (fill in as created, delete before go-live, then check off here)
- [ ] (none yet)
